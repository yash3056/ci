version: 2.1
jobs:
  compile:
   docker:
      - image: fr3akyphantom/droid-builder:latest
   steps:
      - run:
          command: |
           git clone --depth=1 https://github.com/yash3056/android_kernel_xiaomi_mido.git --depth=1 -b wave mido
           cd mido
           wget https://raw.githubusercontent.com/yash3056/ci/main/build.sh
           bash build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
